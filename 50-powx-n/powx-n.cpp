class Solution {
public:
    // double p(double x, double n, double m){

    // }
    double myPow(double x, int n) {
        // if(n==0) return x;
        // if(n>0) n--;
        // else n++;
        // return myPow(, n);
        return pow(x, n);
    }
};